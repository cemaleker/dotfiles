[core]
	excludesfile = ~/.gitignore
	attributesfile = ~/.gitattributes
  editor = mvim -f --remote

[user]
  name = Cemal Eker
  email = ce@idagio.com
  signingkey = D1D8BE93F6431E45

[diff "bin"]
	textconv = hexdump -v -C

[commit]
	gpgsign = true
[color]
	ui = auto

[pager]
	diff = diff-so-fancy | less --tabs=2 -RFX
	show = diff-so-fancy | less --tabs=2 -RFX

[color "diff"]
	meta = yellow bold
	commit = green bold
	frag = magenta bold
	old = red bold
	new = green bold
	whitespace = red reverse
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = "red bold 52"
	newNormal = "green bold"
	newHighlight = "green bold 22"
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "status"]
	added = yellow
	changed = green
	untracked = cyan

# git mergetool
[merge]
	tool = opendiff
	renameLimit = 0

[help]
  autocorrect = 1

[fetch]
	prune = true

[diff]
	compactionHeuristic = true
[remote "origin"]
	fetch = +refs/heads/*:refs/remotes/origin/*
[stash]
	showPatch = true
[log]
	date = relative
